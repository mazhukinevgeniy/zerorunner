package game 
{
	import feathers.core.DisplayListWatcher;
	import starling.display.DisplayObjectContainer;
	
	internal class GameTheme extends DisplayListWatcher
	{
		
		public function GameTheme(gameContainer:DisplayObjectContainer) 
		{
			super(gameContainer);
		}
		
	}

}